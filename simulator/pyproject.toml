[project]
name = "simulator"
version = "0.1.0"
description = "Simulation framework for computer-vision experiments using OpenCV, Ultralytics, and video processing tools."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["opencv-python>=4.8.0", "numpy>=1.24.0", "pyzmq>=26.0.0"]
