[project]
name = "multimodal-drone-detection"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.11"          # Jetpack 6.1 uses 3.10
dependencies = []
[tool.uv.workspace]
members = ["jetson", "backend", "simulator"]

[dependency-groups]
dev = ["pytest-cov>=7.0.0", "ruff>=0.15.1"]

[tool.ruff]
target-version = "py310"
